import 'package:flutter/material.dart';

class YoutubeLivePage extends StatelessWidget {
  const YoutubeLivePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
